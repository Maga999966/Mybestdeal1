name: Build IPA from Release

on:
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download iOS ZIP from Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e

          DOWNLOAD_DIR="$RUNNER_TEMP/ios-download"

          rm -rf "$DOWNLOAD_DIR"
          mkdir -p "$DOWNLOAD_DIR"

          echo "=== RELEASE ASSETS ==="

          gh release view mybestdeal47 \
            --repo "${{ github.repository }}" \
            --json assets \
            --jq '.assets[].name'

          echo
          echo "=== FIND ZIP ==="

          ZIP_NAME=$(gh release view mybestdeal47 \
            --repo "${{ github.repository }}" \
            --json assets \
            --jq '.assets[].name' |
            grep -i '\.zip$' |
            head -1)

          if [ -z "$ZIP_NAME" ]; then
            echo "ERROR: NO ZIP FOUND IN RELEASE"
            exit 1
          fi

          echo "ZIP FOUND:"
          echo "$ZIP_NAME"

          echo
          echo "=== DOWNLOAD ==="

          gh release download mybestdeal47 \
            --repo "${{ github.repository }}" \
            --pattern "$ZIP_NAME" \
            --dir "$DOWNLOAD_DIR"

          ZIP=$(find "$DOWNLOAD_DIR" \
            -type f \
            -iname "*.zip" \
            -print -quit)

          if [ -z "$ZIP" ]; then
            echo "ERROR: ZIP DOWNLOAD FAILED"
            exit 1
          fi

          echo "IOS_ZIP=$ZIP" >> "$GITHUB_ENV"

          echo
          echo "=== ZIP DOWNLOADED ==="
          ls -lh "$ZIP"

      - name: Extract iOS ZIP
        shell: bash
        run: |
          set -e

          IOS_DIR="$RUNNER_TEMP/ios-project"

          rm -rf "$IOS_DIR"
          mkdir -p "$IOS_DIR"

          echo "=== EXTRACT IOS ZIP ==="

          unzip -q -o "$IOS_ZIP" \
            -d "$IOS_DIR"

          echo
          echo "=== EXTRACTED ==="

          find "$IOS_DIR" \
            -maxdepth 8 \
            -print

      - name: Find Xcode project
        shell: bash
        run: |
          set -e

          echo "=== FIND XCODE PROJECT ==="

          XCODEPROJ=$(find "$RUNNER_TEMP/ios-project" \
            -type d \
            -name "*.xcodeproj" \
            -print -quit)

          if [ -z "$XCODEPROJ" ]; then
            echo "ERROR: XCODE PROJECT NOT FOUND"

            find "$RUNNER_TEMP/ios-project" \
              -maxdepth 10 \
              -print

            exit 1
          fi

          echo "XCODEPROJ=$XCODEPROJ" >> "$GITHUB_ENV"

          echo
          echo "=== XCODE PROJECT FOUND ==="
          echo "$XCODEPROJ"

      - name: Show Xcode information
        shell: bash
        run: |
          set -e

          echo "=== XCODE VERSION ==="

          xcodebuild -version

          echo
          echo "=== PROJECT INFORMATION ==="

          xcodebuild \
            -project "$XCODEPROJ" \
            -list

      - name: Find Xcode scheme
        shell: bash
        run: |
          set -e

          echo "=== FIND SCHEME ==="

          PROJECT_INFO=$(xcodebuild \
            -project "$XCODEPROJ" \
            -list)

          echo "$PROJECT_INFO"

          SCHEME=$(echo "$PROJECT_INFO" |
            awk '
              /Schemes:/ {
                found=1
                next
              }

              found && NF {
                gsub(/^[ \t]+|[ \t]+$/, "")
                print
                exit
              }
            ')

          if [ -z "$SCHEME" ]; then
            echo "ERROR: SCHEME NOT FOUND"
            exit 1
          fi

          echo "SCHEME=$SCHEME" >> "$GITHUB_ENV"

          echo
          echo "=== SCHEME FOUND ==="
          echo "$SCHEME"

      - name: Build iOS application
        shell: bash
        run: |
          set -o pipefail

          echo "=== BUILD IOS APPLICATION ==="

          BUILD_DIR="$RUNNER_TEMP/build"

          rm -rf "$BUILD_DIR"
          mkdir -p "$BUILD_DIR"

          xcodebuild \
            -project "$XCODEPROJ" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath "$BUILD_DIR" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build

          echo
          echo "=== BUILD FINISHED ==="

      - name: Find built application
        shell: bash
        run: |
          set -e

          echo "=== FIND APP ==="

          APP=$(find "$RUNNER_TEMP/build/Build/Products" \
            -type d \
            -name "*.app" \
            -print -quit)

          if [ -z "$APP" ]; then
            echo "ERROR: APP NOT FOUND"

            find "$RUNNER_TEMP/build" \
              -maxdepth 10 \
              -print

            exit 1
          fi

          echo "APP=$APP" >> "$GITHUB_ENV"

          echo
          echo "=== APP FOUND ==="
          echo "$APP"

      - name: Create unsigned IPA
        shell: bash
        run: |
          set -e

          echo "=== CREATE IPA ==="

          PAYLOAD="$RUNNER_TEMP/Payload"
          IPA="$RUNNER_TEMP/MyBestDeal-4.7.ipa"

          rm -rf "$PAYLOAD"
          rm -f "$IPA"

          mkdir -p "$PAYLOAD"

          cp -R "$APP" "$PAYLOAD/"

          cd "$RUNNER_TEMP"

          ditto -c -k \
            --sequesterRsrc \
            --keepParent \
            Payload \
            "$IPA"

          if [ ! -f "$IPA" ]; then
            echo "ERROR: IPA NOT CREATED"
            exit 1
          fi

          echo
          echo "=== IPA CREATED ==="

          ls -lh "$IPA"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: MyBestDeal-4.7-IPA
          path: ${{ runner.temp }}/MyBestDeal-4.7.ipa
          if-no-files-found: error

      - name: Upload Xcode project
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: MyBestDeal-4.7-Xcode-Project
          path: ${{ runner.temp }}/ios-project
          if-no-files-found: ignore

      - name: Upload build files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: MyBestDeal-4.7-Build
          path: ${{ runner.temp }}/build
          if-no-files-found: ignore